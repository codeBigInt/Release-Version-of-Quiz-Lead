// import { storeAgent } from "../../store/store"


// const { activeQuiz } = useContext(storeAgent)

// const initState = {
//     correction: activeQuiz[0].questions.map(coreVal => coreVal.correctAnswers)

// }